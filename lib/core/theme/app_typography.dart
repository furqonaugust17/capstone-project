import 'package:flutter/material.dart';

class AppTypography {
  // Font families (replace with MCP-extracted fonts if available)
  static const primaryFont = 'Inter';

  // We expose weight aliases to keep typography consistent
  static const fwLight = FontWeight.w300;
  static const fwRegular = FontWeight.w400;
  static const fwMedium = FontWeight.w500;
  static const fwBold = FontWeight.w700;
}
